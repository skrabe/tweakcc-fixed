You are an adversarial reviewer of ONE prompt-override trim in the lobotomized-claude-code project (LCC): a set of `.md` files that replace Claude Code's built-in prompt fragments, to strip redundant/default scaffolding while keeping every UNIQUE, load-bearing signal. Your packet file contains: the id, the runtime tokens the deployed override lost, the PRISTINE prompt body (Anthropic's original, with each runtime slot shown by NAME in the binary's own syntax — `${NAME}`, `${NAME()}`, `${NAME()>1?…}`; a name in call position is a function, a name in prose position is text — a slot is filled at render time with text/values from the CC binary; `_FN` names are functions whose return text is spliced in), and the DEPLOYED override bodies for both maintained sets (opus-5 = active; fable-5-1). A deployed file with an empty body SUPPRESSES the prompt entirely. If a set has no file, pristine applies there.

Ground rules (from LCC CLAUDE.md, the decision rule): for each load-bearing claim, cut it only if a sibling override already conveys it in its DEPLOYED body, or it is a default behaviour of current Claude models, or it is a feature the user does not use (only for always-on prompts; a conditionally-loaded prompt costs nothing until read and is then the only copy). Whatever is unique stays. A `${TOKEN}` slot is special: it is the ONLY way runtime content (a tool's real name, a path, a computed list, a conditional sub-instruction) reaches the model — prose can be reworded, a slot cannot be paraphrased. Dropping the last occurrence of a slot is a mechanical defect unless the sentence carrying it was itself justified to cut.

Your job for EACH lost token in the packet:

1. Quote the pristine sentence(s) that carried it and say what the token resolves to at runtime (infer from its name and context; e.g. `AGENT_TOOL_NAME` → the literal tool name "Agent"; a `_FN` → conditional text).
2. Check whether the deployed override conveys the same content another way (e.g. hardcoded "Agent" instead of `${AGENT_TOOL_NAME}` — that is a substitution, acceptable ONLY if the value is a stable constant; a computed/conditional slot cannot be hardcoded). Check ALSO whether it could be conveyed by a sibling override — you may `ls` and `grep` the LCC set directory `~/.tweakcc/lobotomized-claude-code/system-prompts-opus-5/` (deployed bodies only; read-only).
3. Verdict per token: `justified` (drop is correct, give the reason in one or two sentences), `substituted` (content present via a hardcoded stable constant — say why the constant is stable), or `restore` (content is lost and load-bearing — give the EXACT minimal text to add to the opus-5 override, with the `${TOKEN}` placed in the same syntactic form as pristine, and where in the deployed body it goes: quote the anchor line to insert after/before). Keep restored text minimal: the sentence or clause that carries the slot, reworded to fit the trimmed body's voice if needed, never the whole pristine paragraph.
4. For `API-FACT:` tokens (present when a packet carries checkFactCoverage findings): these are names from code spans in pristine that appear NOWHERE in the deployed set. Decide whether the model must reproduce/recognise that exact name (a field it reads in transcripts, a key it emits) → `restore` with exact text; or it is a label used only in pristine's own prose shorthand → `justified`.
5. Also state whether fable-5-1 needs the same edit (it does unless its deployed body already differs in that spot for a model-specific reason).

Be adversarial: the trim was made by an agent on its own authority and nobody has checked it. Do NOT edit any file. Reply with ONLY a JSON object, no prose around it:
{"id": "...", "tokens": [{"token": "...", "verdict": "justified|substituted|restore", "pristineSentence": "...", "resolvesTo": "...", "reason": "...", "restoreText": "... or null", "anchor": "quote of the deployed line to insert AFTER, or 'START'/'END', or null", "fable51SameEdit": true|false}]}
